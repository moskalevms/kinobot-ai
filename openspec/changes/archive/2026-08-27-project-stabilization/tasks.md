## 1. Фаза 0 — подготовка и восстановление работоспособности

- [x] 1.1 Закоммитить незакоммиченную работу над админ-панелью отдельным
  коммитом; проверка: `git status` чистый (кроме новых артефактов),
  коммит виден в `git log --oneline`
- [x] 1.2 Исправить импорт в `src/recommendation_engine.py:271` —
  `is_russian_content` брать из плоского импорта в начале файла;
  проверка: вызов `_format_movies_list` из корня проекта через
  `.venv\Scripts\python.exe` возвращает список без ImportError
- [x] 1.3 Завершить переход на плоские импорты во всех модулях `src/`;
  проверка: `python src\telegram_bot.py` запускает polling, бот
  отвечает на `/start` и на запрос рекомендации
- [x] 1.4 Заменить CMD в `Dockerfile` на `python src/telegram_bot.py`;
  проверка: `docker build` успешен, контейнер стартует и в логах
  «Telegram бот запущен в режиме polling»
- [x] 1.5 Проверить веб: `python src\app.py` и `GET /health` возвращает
  200 `healthy`

## 2. Фаза 1 — надёжность: состояние диалога

- [x] 2.1 Добавить модель сессии диалога (последние рекомендации,
  параметры поиска, отметки активности) в `src/models/database.py`;
  проверка: `python init_db.py` создаёт таблицу без ошибок
- [x] 2.2 Перевести `SessionManager` на чтение/запись сессий в
  PostgreSQL с фолбэком на in-memory при ошибке БД; проверка: контекст
  («похожие фильмы» после выдачи) сохраняется после перезапуска бота
  и при двух запущенных процессах веба
- [x] 2.3 Обеспечить фактическое истечение неактивных сессий (1 час):
  периодический вызов очистки; проверка: сессия с последней активностью
  старше часа удаляется (по записи в БД/логе), активная — нет
- [x] 2.4 Изолировать кэш поиска по пользователям (ключ кэша с
  `user_id`), убрать глобальный флаг ошибки поиска; проверка: после
  запроса «похожие» пользователя А кэш пользователя Б продолжает
  отдаваться без нового обращения к Kinopoisk API (по логам «Кэш HIT»)

## 3. Фаза 1 — надёжность: интеграции

- [x] 3.1 Удалить режим `BOT_MODE=webhook`: ветку в `app.py`,
  `/telegram-webhook`, `setup_webhook`; `main()` в `telegram_bot.py`
  использует `create_telegram_app()` вместо дублирующей регистрации;
  проверка: бот запускается, эндпоинта `/telegram-webhook` нет,
  хендлеры регистрируются один раз
- [x] 3.2 Ретраи `gigachat_client.py` — только временные ошибки
  (429/5xx, сетевые) через существующий предикат; проверка: ошибка
  авторизации (401) не ретраится, сразу уходит в фолбэк `LLMRouter`
- [x] 3.3 Ретраи `kinopoisk_client.py` — аналогично (404/400 без
  повторов); проверка: в логах одна попытка для постоянных ошибок
- [x] 3.4 Удалить `src/monitoring.py`; проверка: файла нет, поиск
  «monitoring» по коду пуст

## 4. Фаза 2 — качество

- [x] 4.1 Почистить `requirements.txt` (убрать `pandas`, `numpy`,
  `requests`, дубль `gunicorn`); проверка: установка в чистое
  окружение успешна, бот и веб запускаются
- [x] 4.2 Создать `requirements-dev.txt` (`pytest`, `ruff`, `mypy`);
  проверка: `pip install -r requirements-dev.txt` успешен
- [x] 4.3 Написать тесты `utils/movie_filter.py`, fallback-классификации
  `intent_classifier.py` и веток `dialogue_manager` без внешних API
  (моки Kinopoisk/LLM); проверка: `pytest` зелёный
- [x] 4.4 Настроить и прогнать `ruff` и `mypy` для `src/`, исправить
  ошибки (или зафиксировать осознанные исключения в конфиге);
  проверка: `ruff check src` и `mypy src` проходят
- [x] 4.5 Добавить GitHub Actions: ruff + mypy + pytest (и сборка
  образа); проверка: пайплайн зелёный в ветке изменения
- [x] 4.6 Единая инициализация БД: `init_db.py` импортирует модели из
  `src/models/database.py`; дублирующие определения и flask-команду
  `init_db` удалить; проверка: на чистой БД `python init_db.py`
  создаёт все таблицы и админа
- [x] 4.7 Текущий год — один источник: `CURRENT_YEAR` в `config.py`
  от системной даты, заменить все хардкоды; проверка: поиск «2025»
  в `src/` находит только примеры в промптах
- [x] 4.8 Логирование: идемпотентный `setup_logging` (без дублей
  хендлеров), именованные логгеры, каталог из `LOG_DIR`; проверка:
  при двух вызовах `setup_logging` строки в логе не дублируются
- [x] 4.9 Привести промпт в соответствие с кодом: убрать интенты
  `newer`/`older` (если не реализуются); проверка: все интенты из
  промпта обрабатываются в `dialogue_manager.process_message`

## 5. Фаза 3 — безопасность и гигиена

- [x] 5.1 `init_db.py`: `ADMIN_PASSWORD` обязателен (без него — ошибка
  и выход), пароль не выводится в консоль; проверка: запуск без
  переменной завершается ошибкой, с переменной — в выводе нет пароля
- [x] 5.2 `app.py`: `FLASK_SECRET_KEY` обязателен при
  `FLASK_ENV=production` (fail-fast), в разработке — временный ключ
  с предупреждением; убрать `debug=True` из продакшен-запуска;
  проверка: продакшен-запуск без ключа не стартует и сообщает причину
- [x] 5.3 Удалить легаси: `data/processed/`, `src/models/movie.py`,
  `src/utils/code_collector.py`; проверка: файлов нет, импортов их
  в коде нет
- [x] 5.4 Переписать `README.md` под реальное состояние проекта,
  актуализировать `docs/` (модель импортов, запрет запуска с `-m`,
  деплой); проверка: инструкции из README воспроизводят локальный
  запуск бота и веба
- [x] 5.5 Уточнить и зафиксировать в `docs/` состав сервисов на VM
  (бот / бот+веб) и наличие там `ADMIN_PASSWORD`, `FLASK_SECRET_KEY`;
  проверка: запись в `docs/` с датой подтверждения
